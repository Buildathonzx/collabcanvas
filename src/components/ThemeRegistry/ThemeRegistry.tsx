














}  );    </ThemeProvider>      {children}      <CssBaseline />    <ThemeProvider theme={theme}>  return (export default function ThemeRegistry({ children }: { children: ReactNode }) {import theme from '../../theme/theme';import { ReactNode } from 'react';import CssBaseline from '@mui/material/CssBaseline';import { ThemeProvider } from '@mui/material/styles';'use client';